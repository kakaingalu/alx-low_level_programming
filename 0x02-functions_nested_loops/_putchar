#include <unistd.h>

/**
 *_putchar - entry point
 * @c: The character to print
 *
 *
 * description: writes the character c to stdout
 * Return: On success 1
 * Descritption:On error, -l is returned, and errno is set appropriately
 */
int _putchar(char c)
{
  return (write(1, &c, 1));
}
